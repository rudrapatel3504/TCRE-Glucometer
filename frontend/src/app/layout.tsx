import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { TooltipProvider } from "@/components/ui/tooltip";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TCRE Dashboard - Temporal Clinical Reasoning Engine",
  description: "Temporal Clinical Reasoning Engine (TCRE) MVP v1 Frontend - Transforms temporal blood glucose measurements into clinical insights, confidence-weighted risk scores, and explainable reasoning.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-bg-primary text-text-primary">
        <TooltipProvider>{children}</TooltipProvider>
      </body>
    </html>
  );
}
